---
/*
 * Speaking & Community JSON schema reference (src/data/speaking-community.json):
 *
 * conference_talks / workshops:
 *   { title, event, location, date (YYYY-MM-DD), video?, slides?,
 *     link?: string, links?: string[] | {title,url}[],
 *     photos?: [{ src: filename, caption, hero? }], featured?, description? }
 *
 * media_podcasts:
 *   { title, show, host?, date, type, link, article?, featured?, description? }
 *
 * community_organizing:
 *   { title, role, organization, period, location, website?, featured?,
 *     description?, links?: {title,url}[],
 *     activities?: [{ title, date?, period?, role?, link?, description? }],
 *     photos?: [{ src: filename, caption, hero? }] }
 *
 * photos.src is a bare filename — resolved at build time via import.meta.glob
 */
import Layout from '../layouts/Layout.astro';
import { Image, Picture } from 'astro:assets';
import profileImage from '../../public/profile.jpg';

import scbcn25 from '../assets/photos/SCBCN25 - Documenting APIs.jpeg';
import scbcn24 from '../assets/photos/SCBCN24 - AI in Support teams.jpeg';
import scbcn23 from '../assets/photos/SCBCN23 - We\'re capitalists.jpeg';
import scbcn22 from '../assets/photos/SCBCN22 - NoMeetings Open Space.jpeg';
import cas22Graphic from '../assets/photos/CAS22 - NoMeetings - w graphic recorder.jpeg';
import cas22 from '../assets/photos/CAS22 - NoMeetings.jpeg';
import aos23 from '../assets/photos/AOS23 - Event organizers.jpeg';


import speakingData from '../data/speaking-community.json';

const { conference_talks, workshops, media_podcasts, community_organizing } = speakingData;

function sortByDate(items) {
  return [...items].sort((a, b) => {
    if (!a.date) return 1;
    if (!b.date) return -1;
    return new Date(b.date) - new Date(a.date);
  });
}

function getYear(dateStr) {
  if (!dateStr) return '';
  return new Date(dateStr).getFullYear();
}

function splitItems(items) {
  const sorted = sortByDate(items);
  const featured = sorted.filter(i => i.featured === true);
  const visible = featured.length > 0 ? featured : sorted.slice(0, 3);
  const visibleSet = new Set(visible);
  const hidden = sorted.filter(i => !visibleSet.has(i));
  return { visible, hidden };
}

const talks = splitItems(conference_talks);
const ws = splitItems(workshops);
const media = splitItems(media_podcasts ?? []);
const community = splitItems(community_organizing);
---

<Layout title="Javi Rubio">
  <div class="content-container">

    <!-- HERO -->
    <section class="hero">
      <div class="hero-header">
        <div>
          <h1>Javi Rubio</h1>
          <p class="typed-roles"><span id="typed-role"></span></p>
          <p class="subtitle">Helping teams build better products, knowledge systems, and sustainable work cultures.</p>
        </div>
        <Image src={profileImage} alt="Javi Rubio" class="profile-photo" width={200} height={200} />
      </div>
    </section>

    <!-- ABOUT -->
    <section class="about">
      <p class="intro">
        Software engineer with techno-activist leanings. 20 years working at the intersection of product, engineering, and knowledge management.
      </p>
      <p class="intro">
        I've been part of startups from day one, served as CTO, led complex technical products… Known for championing <a href="https://nomeetings.work" target="_blank" rel="noopener noreferrer"><strong>#NoMeetings</strong></a>, helping distributed teams rethink collaboration beyond default rituals.
      </p>
      <p class="intro">
        Available for speaking at tech conferences and corporate events, defining and implementing AI strategies, and improving collaboration practices.
      </p>
    </section>

    <!-- SPEAKING & COMMUNITY -->
    <section id="speaking" class="speaking-section">
      <h2>Speaking &amp; Community</h2>
      <p class="section-intro">I regularly speak at tech conferences, facilitate workshops, and organize developer communities.</p>

      <!-- Hero photo carousel -->
      <div class="photo-carousel" aria-hidden="true">
        <div class="photo-slide landscape">
          <Picture src={scbcn25} alt="Speaking at SCBCN25" widths={[360, 720]} sizes="360px" formats={['avif', 'webp']} />
          <span class="photo-caption">SCBCN25 - Documenting APIs</span>
        </div>
        <div class="photo-slide portrait">
          <Picture src={scbcn24} alt="Speaking at SCBCN24" widths={[280, 560]} sizes="280px" formats={['avif', 'webp']} />
          <span class="photo-caption">SCBCN24 - AI in Support teams</span>
        </div>
        <div class="photo-slide landscape">
          <Picture src={scbcn23} alt="Speaking at SCBCN23" widths={[360, 720]} sizes="360px" formats={['avif', 'webp']} />
          <span class="photo-caption">SCBCN23 - We're capitalists</span>
        </div>
        <div class="photo-slide portrait">
          <Picture src={scbcn22} alt="Speaking at SCBCN22" widths={[280, 560]} sizes="280px" formats={['avif', 'webp']} />
          <span class="photo-caption">SCBCN22 - NoMeetings Open Space</span>
        </div>
        <div class="photo-slide portrait">
          <Picture src={cas22Graphic} alt="Speaking at CAS22" widths={[280, 560]} sizes="280px" formats={['avif', 'webp']} />
          <span class="photo-caption">CAS22 - NoMeetings & bgcampa</span>
        </div>
        <div class="photo-slide landscape">
          <Picture src={cas22} alt="Speaking at CAS22" widths={[360, 720]} sizes="360px" formats={['avif', 'webp']} />
          <span class="photo-caption">CAS22 - NoMeetings</span>
        </div>
        <div class="photo-slide portrait">
          <Picture src={aos23} alt="AOS23 event organizers" widths={[280, 560]} sizes="280px" formats={['avif', 'webp']} />
          <span class="photo-caption">AOS23 - Event organizers</span>
        </div>
      </div>

      <!-- Conference Talks -->
      <div class="subsection">
        <h3>Conference Talks</h3>
        <ul class="talks-list-inline">
          {conference_talks.length === 0 ? (
            <li class="empty-state">Coming soon.</li>
          ) : (
            <>
              {talks.visible.map(talk => (
                <li class="talk">
                  <strong>
                    {talk.video
                      ? <a href={talk.video} target="_blank" rel="noopener noreferrer">{talk.title}</a>
                      : talk.title}
                  </strong>
                  {' · '}{talk.event}{talk.date ? ` · ${getYear(talk.date)}` : ''}
                  {(talk.video || talk.slides || talk.links?.length > 0) && (
                    <span class="item-links">
                      {talk.video && <a href={talk.video} target="_blank" rel="noopener noreferrer">Video</a>}
                      {talk.slides && <a href={talk.slides} target="_blank" rel="noopener noreferrer">Slides</a>}
                      {talk.links?.map(l => <a href={l.url} target="_blank" rel="noopener noreferrer">{l.title}</a>)}
                    </span>
                  )}
                </li>
              ))}
              {talks.hidden.map(talk => (
                <li class="talk" data-section="talks" style="display:none">
                  <strong>
                    {talk.video
                      ? <a href={talk.video} target="_blank" rel="noopener noreferrer">{talk.title}</a>
                      : talk.title}
                  </strong>
                  {' · '}{talk.event}{talk.date ? ` · ${getYear(talk.date)}` : ''}
                  {(talk.video || talk.slides || talk.links?.length > 0) && (
                    <span class="item-links">
                      {talk.video && <a href={talk.video} target="_blank" rel="noopener noreferrer">Video</a>}
                      {talk.slides && <a href={talk.slides} target="_blank" rel="noopener noreferrer">Slides</a>}
                      {talk.links?.map(l => <a href={l.url} target="_blank" rel="noopener noreferrer">{l.title}</a>)}
                    </span>
                  )}
                </li>
              ))}
            </>
          )}
        </ul>
        {talks.hidden.length > 0 && (
          <button
            class="show-toggle"
            data-toggle
            data-section="talks"
            data-total={talks.hidden.length + talks.visible.length}
            data-label="talks"
          >
            Show all {talks.hidden.length + talks.visible.length} talks
          </button>
        )}
      </div>

      <!-- Workshops & Facilitation -->
      <div class="subsection">
        <h3>Workshops &amp; Facilitation</h3>
        <ul class="talks-list-inline">
          {workshops.length === 0 ? (
            <li class="empty-state">Coming soon.</li>
          ) : (
            <>
              {ws.visible.map(item => (
                <li class="talk">
                  <strong>{item.title}</strong>
                  {' · '}{item.event}{item.date ? ` · ${getYear(item.date)}` : ''}
                  {(item.video || item.slides || item.links?.length > 0) && (
                    <span class="item-links">
                      {item.video && <a href={item.video} target="_blank" rel="noopener noreferrer">Video</a>}
                      {item.slides && <a href={item.slides} target="_blank" rel="noopener noreferrer">Slides</a>}
                      {item.links?.map(l => <a href={l.url} target="_blank" rel="noopener noreferrer">{l.title}</a>)}
                    </span>
                  )}
                </li>
              ))}
              {ws.hidden.map(item => (
                <li class="talk" data-section="workshops" style="display:none">
                  <strong>{item.title}</strong>
                  {' · '}{item.event}{item.date ? ` · ${getYear(item.date)}` : ''}
                  {(item.video || item.slides || item.links?.length > 0) && (
                    <span class="item-links">
                      {item.video && <a href={item.video} target="_blank" rel="noopener noreferrer">Video</a>}
                      {item.slides && <a href={item.slides} target="_blank" rel="noopener noreferrer">Slides</a>}
                      {item.links?.map(l => <a href={l.url} target="_blank" rel="noopener noreferrer">{l.title}</a>)}
                    </span>
                  )}
                </li>
              ))}
            </>
          )}
        </ul>
        {ws.hidden.length > 0 && (
          <button
            class="show-toggle"
            data-toggle
            data-section="workshops"
            data-total={ws.hidden.length + ws.visible.length}
            data-label="workshops"
          >
            Show all {ws.hidden.length + ws.visible.length} workshops
          </button>
        )}
      </div>

      <!-- Media & Podcasts -->
      <div class="subsection">
        <h3>Media &amp; Podcasts</h3>
        <ul class="talks-list-inline">
          {(media_podcasts ?? []).length === 0 ? (
            <li class="empty-state">Coming soon.</li>
          ) : (
            <>
              {media.visible.map(item => (
                <li class="talk">
                  <strong>
                    <a href={item.link} target="_blank" rel="noopener noreferrer">{item.title}</a>
                  </strong>
                  {' · '}{item.show}{item.host ? ` · with ${item.host}` : ''}{item.date ? ` · ${getYear(item.date)}` : ''}
                  {item.article && (
                    <span class="item-links">
                      <a href={item.article} target="_blank" rel="noopener noreferrer">Article</a>
                    </span>
                  )}
                </li>
              ))}
              {media.hidden.map(item => (
                <li class="talk" data-section="media" style="display:none">
                  <strong>
                    <a href={item.link} target="_blank" rel="noopener noreferrer">{item.title}</a>
                  </strong>
                  {' · '}{item.show}{item.host ? ` · with ${item.host}` : ''}{item.date ? ` · ${getYear(item.date)}` : ''}
                  {item.article && (
                    <span class="item-links">
                      <a href={item.article} target="_blank" rel="noopener noreferrer">Article</a>
                    </span>
                  )}
                </li>
              ))}
            </>
          )}
        </ul>
        {media.hidden.length > 0 && (
          <button
            class="show-toggle"
            data-toggle
            data-section="media"
            data-total={media.hidden.length + media.visible.length}
            data-label="items"
          >
            Show all {media.hidden.length + media.visible.length} items
          </button>
        )}
      </div>

      <!-- Community Organizing -->
      <div class="subsection">
        <h3>Community Organizing</h3>
        <ul class="talks-list-inline">
          {community_organizing.length === 0 ? (
            <li class="empty-state">Coming soon.</li>
          ) : (
            <>
              {community.visible.map(item => (
                <li class="talk">
                  <strong>
                    {item.website
                      ? <a href={item.website} target="_blank" rel="noopener noreferrer">{item.title || item.organization}</a>
                      : (item.title || item.organization)}
                  </strong>
                  {' · '}{item.role}{item.period ? ` · ${item.period}` : ''}
                  {item.location && <span class="item-meta">{item.location}</span>}
                  {item.links?.length > 0 && (
                    <span class="item-links">
                      {item.links.map(l => (
                        <a href={l.url} target="_blank" rel="noopener noreferrer">{l.title}</a>
                      ))}
                    </span>
                  )}
                  {item.activities?.length > 0 && (
                    <ul class="activities-list">
                      {item.activities.map(act => (
                        <li>
                          {act.link
                            ? <a href={act.link} target="_blank" rel="noopener noreferrer">{act.title}</a>
                            : act.title}
                          {act.date ? ` · ${getYear(act.date)}` : ''}
                          {act.role ? ` · ${act.role}` : ''}
                        </li>
                      ))}
                    </ul>
                  )}
                </li>
              ))}
              {community.hidden.map(item => (
                <li class="talk" data-section="community" style="display:none">
                  <strong>
                    {item.website
                      ? <a href={item.website} target="_blank" rel="noopener noreferrer">{item.title || item.organization}</a>
                      : (item.title || item.organization)}
                  </strong>
                  {' · '}{item.role}{item.period ? ` · ${item.period}` : ''}
                  {item.location && <span class="item-meta">{item.location}</span>}
                  {item.links?.length > 0 && (
                    <span class="item-links">
                      {item.links.map(l => (
                        <a href={l.url} target="_blank" rel="noopener noreferrer">{l.title}</a>
                      ))}
                    </span>
                  )}
                  {item.activities?.length > 0 && (
                    <ul class="activities-list">
                      {item.activities.map(act => (
                        <li>
                          {act.link
                            ? <a href={act.link} target="_blank" rel="noopener noreferrer">{act.title}</a>
                            : act.title}
                          {act.date ? ` · ${getYear(act.date)}` : ''}
                          {act.role ? ` · ${act.role}` : ''}
                        </li>
                      ))}
                    </ul>
                  )}
                </li>
              ))}
            </>
          )}
        </ul>
        {community.hidden.length > 0 && (
          <button
            class="show-toggle"
            data-toggle
            data-section="community"
            data-total={community.hidden.length + community.visible.length}
            data-label="organizations"
          >
            Show all {community.hidden.length + community.visible.length} organizations
          </button>
        )}
      </div>

    </section>
  </div>

</Layout>

<script>
  import Typed from 'typed.js';

  new Typed('#typed-role', {
    strings: [
      'Tech Writer',
      'Developer Relations',
      'Chief Technical Officer',
      'Associate Professor',
      'Android Dev',
      'Technical Coach',
      'Technical Product Manager',
      'Product Engineer',
    ],
    typeSpeed: 60,
    backSpeed: 30,
    backDelay: 1600,
    loop: true,
    showCursor: true,
    cursorChar: '|',
    shuffle: true,
  });

  // Show/hide subsection items (hidden via inline style on load)
  document.querySelectorAll('[data-toggle]').forEach(btn => {
    const section = (btn as HTMLElement).dataset.section;
    const label = (btn as HTMLElement).dataset.label;
    const total = (btn as HTMLElement).dataset.total;

    btn.addEventListener('click', () => {
      const isExpanded = (btn as HTMLElement).dataset.expanded === 'true';
      document.querySelectorAll(`[data-section="${section}"]`).forEach(item => {
        (item as HTMLElement).style.display = isExpanded ? 'none' : '';
      });
      (btn as HTMLElement).dataset.expanded = isExpanded ? 'false' : 'true';
      btn.textContent = isExpanded ? `Show all ${total} ${label}` : 'Show less';
    });
  });
</script>

<style>
  /* ── Hero ────────────────────────── */
  .hero {
    text-align: left;
  }

  .hero-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .profile-photo {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
    animation: fadeIn 0.8s ease-in;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .typed-roles {
    font-size: 1.3rem;
    font-weight: 500;
    color: var(--color-magenta);
    min-height: 2rem;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.3rem;
    font-weight: 500;
    color: var(--color-black);
  }

  :global(.typed-cursor) {
    color: var(--color-magenta);
    font-weight: 300;
  }

  /* ── Speaking section ─────────────── */
  .speaking-section {
    max-width: 1200px;
    margin-top: 4rem;
    padding-top: 2rem;
    border-top: 1px solid var(--color-gray-light);
  }

  /* Clearfix for carousel float */
  .speaking-section::after {
    content: "";
    display: table;
    clear: both;
  }

  .speaking-section h2 {
    font-size: 2rem;
    margin-bottom: 0.75rem;
  }

  .section-intro {
    font-size: 1.05rem;
    color: var(--color-gray-medium);
    margin-bottom: 1.5rem;
  }

  /* ── Carousel (preserved) ─────────── */
  .photo-carousel {
    float: right;
    width: 360px;
    height: 360px;
    margin-left: 1.5rem;
    margin-bottom: 1.5rem;
    border-radius: 12px;
    overflow: hidden;
    position: relative;
  }

  .photo-slide {
    position: absolute;
    opacity: 0;
    animation: fadeSlide 28s infinite;
    border-radius: 12px;
    overflow: hidden;
  }

  .photo-slide.portrait {
    width: 280px;
    height: 360px;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
  }

  .photo-slide.landscape {
    width: 360px;
    height: 230px;
    top: 50%;
    left: 0;
    transform: translateY(-50%);
  }

  .photo-slide:nth-child(1) { animation-delay: 0s; }
  .photo-slide:nth-child(2) { animation-delay: 4s; }
  .photo-slide:nth-child(3) { animation-delay: 8s; }
  .photo-slide:nth-child(4) { animation-delay: 12s; }
  .photo-slide:nth-child(5) { animation-delay: 16s; }
  .photo-slide:nth-child(6) { animation-delay: 20s; }
  .photo-slide:nth-child(7) { animation-delay: 24s; }

  .photo-slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .photo-caption {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4rem 0.6rem;
    font-size: 0.75rem;
    color: var(--color-gray-medium);
    background: rgba(255, 255, 255, 0.85);
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  @keyframes fadeSlide {
    0%   { opacity: 0; }
    2%   { opacity: 1; }
    12%  { opacity: 1; }
    14%  { opacity: 0; }
    100% { opacity: 0; }
  }

  /* ── Subsections ──────────────────── */
  .subsection {
    margin-top: 3rem;
  }

  .subsection h3 {
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--color-black);
    margin-bottom: 1rem;
  }

  .empty-state {
    color: var(--color-gray-medium);
    font-style: italic;
  }

  .talks-list-inline {
    list-style: none;
    padding-left: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .talk {
    padding-left: 1.5rem;
    position: relative;
  }

  .talk::before {
    content: "–";
    position: absolute;
    left: 0;
    color: var(--color-gray-medium);
  }

  .talk strong {
    font-size: 1.05rem;
    font-weight: 600;
  }

  .item-meta {
    display: block;
    font-size: 0.9rem;
    color: var(--color-gray-medium);
    margin-top: 0.2rem;
  }

  .item-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.3rem;
  }

  .item-links a {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--color-turquoise);
  }

  .item-links a:hover {
    color: var(--color-magenta);
    text-decoration: underline;
  }

  .activities-list {
    list-style: disc;
    padding-left: 1.5rem;
    margin-top: 0.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    font-size: 0.9rem;
    color: var(--color-gray-medium);
  }

  .show-toggle {
    margin-top: 1rem;
    background: none;
    border: 1px solid currentColor;
    border-radius: 4px;
    padding: 0.2rem 0.6rem;
    cursor: pointer;
    color: var(--color-turquoise);
    font-size: 0.85rem;
    font-family: inherit;
  }

  .show-toggle:hover {
    color: var(--color-magenta);
  }

  /* ── Mobile ───────────────────────── */
  @media (max-width: 768px) {
    .hero-header {
      flex-direction: column;
      align-items: center;
      text-align: center;
      margin-bottom: 1rem;
    }

    .profile-photo {
      width: 150px;
      height: 150px;
      order: -1;
    }

    .typed-roles,
    .subtitle {
      font-size: 1.1rem;
    }

    .photo-carousel {
      width: 180px;
      height: 180px;
      margin-left: 1rem;
      margin-bottom: 1rem;
    }

    .photo-slide.portrait {
      width: 140px;
      height: 180px;
    }

    .photo-slide.landscape {
      width: 180px;
      height: 115px;
    }

    .talks-list-inline {
      clear: both;
    }
  }
</style>
